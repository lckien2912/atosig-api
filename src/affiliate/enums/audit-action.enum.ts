export enum AuditAction {
    APPROVE = 'APPROVE',
    REJECT = 'REJECT',
    HOLD = 'HOLD',
    RELEASE = 'RELEASE',
    AUTO_APPROVE = 'AUTO_APPROVE',
    REVERT = 'REVERT',
    MARK_PAID = 'MARK_PAID'
}
